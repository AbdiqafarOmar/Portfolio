import { MapPin } from "lucide-react";
import { experiences } from "../data";
export default function ExperiencePage() { return <main className="page-shell inner-page"><header className="page-heading compact-heading"><p className="eyebrow">Experience</p><h1>Engineering across products, data, and automation.</h1><p>Two internships, one throughline: turning ambiguous workflows into reliable, testable systems.</p></header><section className="experience-list">{experiences.map((item, index) => <article className="experience-row" key={item.role}><div className="experience-order">0{index + 1}</div><div className="experience-main"><div className="experience-labels"><span>{item.period}</span><span><MapPin size={14} />{item.location}</span></div><h2>{item.role}</h2><p className="company">{item.company}</p><p className="experience-intro">{item.intro}</p><ul>{item.bullets.map(bullet => <li key={bullet}>{bullet}</li>)}</ul><div className="chip-row">{item.tags.map(tag => <span key={tag}>{tag}</span>)}</div></div></article>)}</section></main>; }
